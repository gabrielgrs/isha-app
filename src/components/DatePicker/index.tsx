import 'react-datepicker/dist/react-datepicker.css'

import type * as T from './types'
import * as S from './styles'

function DatePicker(props: T.Props) {
  return <S.DatePicker {...props} />
}

export default DatePicker
